# 永久会话功能设计文档

## 文档信息
- **版本**: v1.0
- **创建时间**: 2024
- **状态**: 待审核
- **优先级**: 中

---

## 1. 功能概述

### 1.1 核心概念
为每篇论文（通过DOI标识)创建一个**唯一的、永久的公开讨论空间**，类似于论文的"官方讨论区"。区别于现有的临时公开会话和私密会话，永久会话具有以下核心特征：

- **永久保留**：不可被普通用户删除
- **论文绑定**：与特定DOI唯一绑定
- **无主持人**：所有参与者地位平等
- **完全公开**：任何人可发现和加入

### 1.2 与现有会话类型的区别

| 特性 | 临时公开会话 | 临时私密会话 | 永久论文会话 |
|------|-------------|-------------|-------------|
| **目标用户** | 个人、小群体 | 私密小组 | 学术社区 |
| **创建限制** | 无限制 | 无限制 | 每DOI唯一 |
| **主持人角色** | 有（创建者） | 有（创建者） | 无 |
| **删除权限** | 创建者 | 创建者 | 仅管理员 |
| **生命周期** | 可随时删除 | 可随时删除 | 永久保留 |
| **可见性** | 会话广场可见 | 仅邀请码可见 | 会话广场可见 |
| **加入方式** | 广场浏览/邀请码 | 仅邀请码 | 广场浏览/DOI搜索 |
| **绑定对象** | 论文（可选） | 论文（可选） | 论文（必选） |

---

## 2. 用户场景

### 2.1 主要使用场景
1. **学术讨论**：研究者为某篇重要论文建立长期讨论空间
2. **知识沉淀**：论文的注释、理解、应用案例长期积累
3. **协作研究**：多个研究团队针对同一论文进行持续交流
4. **教学资源**：导师为课程指定论文建立讨论区

### 2.2 用户流程

#### 用户A（首次创建者）
```
1. 在Zotero中选中论文 → 点击"创建会话"按钮
2. 看到3个选项：
   ✨ 创建公开会话（临时，我主持）
   🔒 创建私密会话（临时，邀请制）
   📚 创建永久会话（公开，永久保留）
3. 选择"永久会话" → 填写会话标题和初始话题
4. 提交后成为该论文永久会话的"首次创建者"（获得徽章）
5. 会话立即出现在会话广场和论文详情页
```

#### 用户B（后续加入者）
```
1. 在会话广场浏览 → 看到永久会话（特殊标识 📚）
2. 点击加入 → 无需审批，直接进入
3. 或：在论文详情页看到"已有永久讨论" → 点击加入
```

#### 用户C（尝试重复创建）
```
1. 选中同一篇论文 → 尝试创建永久会话
2. 系统提示："该论文已存在永久会话，是否前往？"
3. 点击"前往" → 自动加入现有会话
```

---

## 3. 技术设计

### 3.1 数据库结构调整

#### 3.1.1 修改 `reading_sessions` 表

**新增/修改字段**：
```sql
-- 会话类型（新增 'permanent' 值）
session_type TEXT NOT NULL CHECK (session_type IN ('public', 'private', 'permanent'))

-- 是否被管理员锁定（新增字段）
is_locked BOOLEAN DEFAULT FALSE

-- 论文DOI（对 permanent 类型必填）
paper_doi TEXT

-- 唯一性约束
CONSTRAINT unique_permanent_session 
  UNIQUE (paper_doi, session_type) 
  WHERE session_type = 'permanent'
```

**字段语义变化**：
- `creator_id`: 对永久会话仍保留，但仅用于统计"首次创建者"，不具有特殊权限
- `is_active`: 对永久会话始终为 `true`（除非管理员软删除）
- `is_locked`: 管理员可锁定会话，禁止新成员加入（保留历史记录但冻结讨论）

#### 3.1.2 RLS 策略调整
```sql
-- 永久会话始终公开可见（除非被锁定）
CREATE POLICY "permanent_sessions_public" ON reading_sessions
  FOR SELECT
  USING (
    session_type = 'permanent' 
    AND is_active = true 
    AND is_locked = false
  );

-- 只有管理员可删除/锁定永久会话
CREATE POLICY "permanent_sessions_admin_only" ON reading_sessions
  FOR UPDATE
  USING (
    session_type = 'permanent' 
    AND auth.jwt() ->> 'role' = 'admin'
  );
```

### 3.2 API 端点设计

#### 3.2.1 创建永久会话
```typescript
// POST /api/proxy/reading-session/create-permanent
Request Body:
{
  paper_doi: string;        // 必填
  session_name: string;     // 会话标题
  initial_topic?: string;   // 初始话题描述
}

Response:
{
  success: boolean;
  data?: {
    session_id: string;
    is_first_creator: boolean;  // 是否首次创建者
  };
  error?: {
    code: 'ALREADY_EXISTS' | 'RATE_LIMIT' | 'VALIDATION_ERROR';
    message: string;
    existing_session_id?: string;  // 已存在会话的ID
  };
}
```

#### 3.2.2 检查论文是否存在永久会话
```typescript
// GET /api/proxy/reading-session/check-permanent?doi=<DOI>
Response:
{
  exists: boolean;
  session?: {
    session_id: string;
    session_name: string;
    created_at: string;
    members_count: number;
  };
}
```

#### 3.2.3 获取永久会话列表
```typescript
// GET /api/proxy/reading-session/list?type=permanent
Response:
{
  sessions: Array<{
    session_id: string;
    session_name: string;
    paper_doi: string;
    paper_title: string;
    created_at: string;
    members_count: number;
    is_locked: boolean;
    first_creator: {
      username: string;
      avatar?: string;
    };
  }>;
}
```

### 3.3 前端组件调整

#### 3.3.1 会话创建表单 (`SessionCreateFormView`)
```typescript
// 新增单选按钮组
<RadioGroup value={sessionType} onChange={setSessionType}>
  <Radio value="public">
    <Icon>✨</Icon>
    <Text>创建公开会话</Text>
    <Description>临时会话，由我主持</Description>
  </Radio>
  
  <Radio value="private">
    <Icon>🔒</Icon>
    <Text>创建私密会话</Text>
    <Description>临时会话，邀请制</Description>
  </Radio>
  
  <Radio value="permanent">
    <Icon>📚</Icon>
    <Text>创建永久会话</Text>
    <Description>公开永久，无主持人</Description>
    <Alert>每篇论文只能创建一个永久会话</Alert>
  </Radio>
</RadioGroup>

// 当选择 permanent 时显示特殊提示
{sessionType === 'permanent' && (
  <InfoBox>
    <h4>永久会话说明：</h4>
    <ul>
      <li>该会话将永久绑定到当前论文</li>
      <li>所有用户平等参与，无主持人角色</li>
      <li>仅管理员可删除，请谨慎创建</li>
      <li>你将成为"首次创建者"并获得徽章</li>
    </ul>
  </InfoBox>
)}
```

#### 3.3.2 会话列表视图（Zotero插件）
```typescript
// 新增"永久会话"标签页
<TabBar>
  <Tab>会话广场</Tab>
  <Tab>我创建的</Tab>
  <Tab>我加入的</Tab>
  <Tab>永久会话</Tab>  // 新增
</TabBar>

// 会话卡片显示永久标识
<SessionCard>
  {session.session_type === 'permanent' && (
    <Badge color="gold">📚 永久</Badge>
  )}
  <Title>{session.name}</Title>
  <Meta>
    <DOI>{session.paper_doi}</DOI>
    <Creator>由 {session.first_creator} 首次创建</Creator>
  </Meta>
</SessionCard>
```

#### 3.3.3 论文详情页增强
```typescript
// 在论文详情页显示永久会话链接
<PaperDetails>
  {/* 现有内容 */}
  
  {hasPermanentSession && (
    <Section>
      <Icon>💬</Icon>
      <Link href={`/session/${permanentSessionId}`}>
        加入该论文的永久讨论
      </Link>
      <Meta>{membersCount} 人参与</Meta>
    </Section>
  )}
  
  {!hasPermanentSession && (
    <Button onClick={createPermanentSession}>
      <Icon>➕</Icon>
      为该论文创建永久会话
    </Button>
  )}
</PaperDetails>
```

---

## 4. 权限与安全

### 4.1 创建限制
- **频率限制**：每个用户每天最多创建 5 个永久会话
- **内容审核**：会话标题必须至少 10 个字符，避免垃圾会话
- **DOI 验证**：必须是有效的 DOI 格式（正则验证）

### 4.2 管理权限
- **管理员操作**：
  - 删除永久会话（软删除，保留历史记录）
  - 锁定会话（禁止新成员加入，现有成员可继续查看）
  - 转移会话到其他DOI（处理DOI错误）
  - 合并重复会话（手动操作）

### 4.3 滥用防范
- **垃圾内容检测**：自动标记空内容/重复标题的会话
- **举报机制**：用户可举报不当会话，管理员审核
- **自动锁定**：超过30天无活动的空会话自动锁定

---

## 5. 用户体验设计

### 5.1 视觉标识
- **永久会话图标**：📚（书本）
- **徽章系统**：
  - 🌟 "论文先驱者"：首次为论文创建永久会话
  - 🎯 "活跃贡献者"：在永久会话中发表超过10条注释
- **状态标识**：
  - 🔓 正常开放
  - 🔒 已锁定（管理员操作）

### 5.2 交互流程优化
1. **智能推荐**：用户查看论文时，自动显示"已有N人在讨论此论文"
2. **快速加入**：从论文详情页一键加入永久会话
3. **重复创建保护**：尝试重复创建时自动跳转到现有会话
4. **离线提示**：Zotero插件中显示永久会话状态（在线人数、最新活动）

### 5.3 通知策略
- 加入永久会话时：不发送通知给其他成员（避免干扰）
- 有新注释时：仅通知@提及的用户
- 会话被锁定时：通知所有成员

---

## 6. 数据统计与激励

### 6.1 统计指标
- **论文维度**：
  - 有永久会话的论文数量
  - 平均会话活跃度
  - 最受欢迎的论文（按会话成员数排序）

- **用户维度**：
  - 首次创建的永久会话数量
  - 参与的永久会话数量
  - 在永久会话中的贡献（注释数、点赞数）

### 6.2 激励机制
- **成就系统**：
  - "开拓者"：创建首个永久会话
  - "社区建设者"：创建10个永久会话
  - "学术达人"：参与50个永久会话

- **排行榜**：
  - 本月最活跃的永久会话
  - 贡献最多的用户
  - 最受欢迎的论文讨论

---

## 7. 实施计划

### Phase 1: 核心功能（优先级：高）
- [ ] 数据库迁移：添加 `session_type='permanent'` 和 `is_locked` 字段
- [ ] 添加唯一性约束 `UNIQUE(paper_doi, session_type)`
- [ ] API端点：`create-permanent`, `check-permanent`
- [ ] 前端：会话创建表单新增"永久会话"选项
- [ ] 前端：重复创建保护逻辑
- [ ] 测试：创建、查重、加入流程

### Phase 2: 展示优化（优先级：中）
- [ ] 会话列表：新增"永久会话"筛选/标签页
- [ ] 会话卡片：永久标识和首次创建者显示
- [ ] 论文详情页：永久会话链接/创建按钮
- [ ] Zotero插件：同步上述UI变更
- [ ] 测试：用户浏览和发现流程

### Phase 3: 管理功能（优先级：中）
- [ ] 管理后台：永久会话列表
- [ ] 管理操作：删除、锁定、解锁
- [ ] 日志记录：管理员操作审计
- [ ] 举报系统：用户举报入口
- [ ] 测试：管理员操作流程

### Phase 4: 激励与统计（优先级：低）
- [ ] 徽章系统：首次创建者标识
- [ ] 统计面板：永久会话相关指标
- [ ] 排行榜：最活跃会话/用户
- [ ] 通知优化：活动提醒策略
- [ ] 测试：统计准确性和性能

---

## 8. 潜在问题与解决方案

### Q1: 如何防止恶意用户批量创建空会话？
**方案**：
- 每天创建数量限制（5个）
- 创建时必须填写会话标题（≥10字符）
- 自动检测重复标题，要求修改
- 管理后台可批量锁定/删除垃圾会话

### Q2: 如何处理DOI错误或论文更新？
**方案**：
- 管理员可"转移"永久会话到新DOI
- 保留操作日志，避免滥用
- 用户可举报DOI错误，管理员审核

### Q3: 如何处理多人同时创建同一论文的永久会话？
**方案**：
- 数据库唯一性约束自动拒绝后创建的请求
- 前端显示友好错误信息："该论文已存在永久会话"
- 提供"立即加入"按钮，引导用户进入现有会话

### Q4: 永久会话是否应支持"归档"功能？
**方案**：
- 初期不支持，所有永久会话持续可见
- 如有需要，Phase 3 可添加管理员"归档"功能：
  - 归档后不在会话广场显示
  - 但可通过DOI搜索找到
  - 成员仍可查看历史内容

### Q5: 首次创建者是否应获得特殊权限？
**方案**：
- **不应获得管理权限**（与设计初衷冲突）
- **应获得荣誉标识**："论文先驱者"徽章
- **统计维度**：个人主页显示"首次创建的永久会话"列表
- **未来扩展**：社区声誉系统中给予额外积分

### Q6: 如何处理论文被撤稿的情况？
**方案**：
- 管理员锁定会话并添加"论文已撤稿"警告
- 历史讨论内容保留（学术透明度）
- 禁止新成员加入
- 在会话顶部显示明显警告横幅

---

## 9. 成功指标

### 9.1 技术指标
- [ ] 永久会话创建成功率 > 99%
- [ ] 重复创建拦截率 = 100%
- [ ] API 响应时间 < 200ms
- [ ] 数据库查询唯一性检查 < 50ms

### 9.2 业务指标
- [ ] 永久会话数量在上线后3个月达到 100+
- [ ] 平均每个永久会话有 5+ 名成员
- [ ] 用户反馈满意度 > 4.5/5
- [ ] 垃圾会话比例 < 5%

### 9.3 用户体验指标
- [ ] 从论文详情页到加入永久会话 < 2 次点击
- [ ] 重复创建保护提示清晰度 > 4/5
- [ ] 管理后台操作便捷性 > 4/5

---

## 10. 附录

### 10.1 相关文档
- [项目架构设计](../ARCHITECTURE.md)
- [API 文档](../API.md)
- [用户指南](../USER-GUIDE.md)

### 10.2 术语表
- **永久会话**：与特定DOI唯一绑定的、永久保留的公开讨论空间
- **首次创建者**：第一个为某论文创建永久会话的用户
- **会话锁定**：管理员禁止新成员加入会话的操作
- **软删除**：标记会话为不可见但保留历史数据

### 10.3 待讨论问题
1. 永久会话是否需要"置顶"功能？
2. 是否允许用户"关注"某篇论文的永久会话？
3. 是否需要为永久会话添加"标签"系统（如"综述"、"方法论"等）？
4. 管理员删除永久会话时，是否需要通知所有成员？
5. 是否需要支持"请求合并会话"功能（用户发现重复会话时）？

---

**文档结束**
