# 14.1. Chrome DevTools MCP 官方指南

> **来源**: Google Chrome 官方发布  
> **发布日期**: 2025-09-23  
> **版本**: v0.12.1  
> **仓库**: https://github.com/ChromeDevTools/chrome-devtools-mcp

---

## 一、概述

**Chrome DevTools MCP** 是 Google Chrome 团队官方发布的 MCP Server，让 AI 编程助手（Gemini、Claude、Cursor、Copilot 等）能够直接控制和检查 Chrome 浏览器。

**核心能力**：
- 🔍 性能分析 - 录制性能 trace，提取 Core Web Vitals
- 🌐 网络调试 - 分析请求、诊断 CORS 问题
- 📸 截图/快照 - 捕获页面视觉状态和 DOM 结构
- 🖱️ 输入自动化 - 点击、填写、拖拽、键盘操作

---

## 二、快速配置

### 环境要求

- Node.js v20.19+ (LTS)
- Chrome 稳定版
- npm

### MCP Client 配置

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"]
    }
  }
}
```

**配置文件位置**：

| 客户端 | 配置路径 |
|--------|----------|
| VS Code Copilot | `.vscode/settings.json` → `mcp.servers` |
| Claude Desktop | `%APPDATA%\Claude\claude_desktop_config.json` |
| Cursor | Settings → MCP |

### 验证安装

在 AI 助手中输入：
```
Check the performance of https://web.dev
```

AI 应自动打开浏览器并返回性能分析结果。

---

## 三、26 个工具概览

### 输入自动化 (8 个)

| 工具 | 功能 |
|------|------|
| `click` | 点击元素（支持双击） |
| `fill` | 填写输入框/选择下拉框 |
| `fill_form` | 批量填写表单 |
| `drag` | 拖拽元素 |
| `hover` | 悬停 |
| `press_key` | 按键（支持组合键如 `Ctrl+A`） |
| `handle_dialog` | 处理对话框 |
| `upload_file` | 上传文件 |

### 导航自动化 (6 个)

| 工具 | 功能 |
|------|------|
| `new_page` | 新建页面 |
| `navigate_page` | 导航（URL/前进/后退/刷新） |
| `list_pages` | 列出所有页面 |
| `select_page` | 切换页面 |
| `close_page` | 关闭页面 |
| `wait_for` | 等待文本出现 |

### 性能分析 (3 个)

| 工具 | 功能 |
|------|------|
| `performance_start_trace` | 开始性能录制 |
| `performance_stop_trace` | 停止录制 |
| `performance_analyze_insight` | 分析性能洞察 |

### 网络调试 (2 个)

| 工具 | 功能 |
|------|------|
| `list_network_requests` | 列出网络请求 |
| `get_network_request` | 获取请求详情 |

### 调试工具 (5 个)

| 工具 | 功能 |
|------|------|
| `take_screenshot` | 截图（全页/元素） |
| `take_snapshot` | 获取 A11Y 树快照 |
| `evaluate_script` | 执行 JavaScript |
| `list_console_messages` | 列出控制台日志 |
| `get_console_message` | 获取日志详情 |

### 模拟 (2 个)

| 工具 | 功能 |
|------|------|
| `emulate` | 模拟网络/地理位置/CPU |
| `resize_page` | 调整视口大小 |

---

## 四、典型使用场景

### 场景 1: 实时验证代码修改

```
Verify in the browser that your change works as expected.
```

### 场景 2: 诊断图片加载失败

```
A few images on localhost:8080 are not loading. What's happening?
```

### 场景 3: 表单提交调试

```
Why does submitting the form fail after entering an email address?
```

### 场景 4: 样式布局问题

```
The page on localhost:8080 looks strange and off. Check what's happening there.
```

### 场景 5: 性能优化

```
Localhost:8080 is loading slowly. Make it load faster.
```

---

## 五、高级配置

### 常用启动参数

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": [
        "chrome-devtools-mcp@latest",
        "--headless=true",      // 无头模式
        "--isolated=true",      // 隔离环境(临时用户目录)
        "--channel=canary",     // Chrome 渠道
        "--viewport=1280x720"   // 视口大小
      ]
    }
  }
}
```

### 连接到已运行的 Chrome

**方法 A: 自动连接（Chrome 145+）**

1. Chrome 访问 `chrome://inspect/#remote-debugging` 启用调试
2. 配置 MCP：`--autoConnect --channel=beta`

**方法 B: 手动端口连接**

1. 启动 Chrome：
   ```powershell
   # Windows
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="%TEMP%\chrome-profile"
   ```

2. 配置 MCP：
   ```json
   "args": ["chrome-devtools-mcp@latest", "--browser-url=http://127.0.0.1:9222"]
   ```

---

## 六、安全注意事项

⚠️ **重要警告**：

1. MCP Server 会暴露浏览器内容给 AI 客户端
2. **不要在调试会话中访问敏感网站**（银行、邮箱等）
3. 使用 `--isolated=true` 创建隔离环境
4. 远程调试端口打开时，任何本机程序都可连接

---

## 七、参考链接

| 资源 | 链接 |
|------|------|
| 官方博客 | https://developer.chrome.com/blog/chrome-devtools-mcp |
| GitHub 仓库 | https://github.com/ChromeDevTools/chrome-devtools-mcp |
| 工具参考 | https://github.com/ChromeDevTools/chrome-devtools-mcp/blob/main/docs/tool-reference.md |
| 故障排除 | https://github.com/ChromeDevTools/chrome-devtools-mcp/blob/main/docs/troubleshooting.md |

---

**文档版本**: v1.0  
**最后更新**: 2026-01-06
