# ==============================================================
# Researchopia Environment Variables Template
# ==============================================================
# Usage: Copy this file and rename to .env.local, then fill in real values
# Warning: .env.local contains sensitive secrets and is excluded in .gitignore
# ==============================================================

# ==================== 1. Supabase (Required) ====================
# Get from: https://supabase.com/dashboard > Project > Settings > API
#
# NEXT_PUBLIC_* variables are exposed to the browser; anon key is safe to expose
# SERVICE_ROLE_KEY is server-side only; never expose to client

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ==================== 2. Site Configuration (Required) ====================
# Callback URL for email verification, password reset, etc.
# Use http://localhost:3000 for development, actual domain for production

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==================== 3. Email Service - Resend (Recommended) ====================
# Get from: https://resend.com/api-keys
#
# Resend is the recommended email service, simpler and more reliable than SMTP
# Free tier: 100 emails/day, 3000 emails/month

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=Researchopia

# ==================== 4. Google reCAPTCHA v3 (Recommended) ====================
# Get from: https://www.google.com/recaptcha/admin
#
# Prevents bot registrations and malicious requests
# Choose reCAPTCHA v3 (invisible verification)

NEXT_PUBLIC_RECAPTCHA_SITE_KEY=6Lxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RECAPTCHA_SECRET_KEY=6Lxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ==================== 5. Email Validation API (Optional) ====================
# Get from: https://www.abstractapi.com/email-validation-api
#
# Validates email authenticity (prevents disposable emails)
# Free tier: 100 requests/month

EMAIL_VALIDATION_API_KEY=your_abstract_api_key

# ==================== 6. Security Configuration (Required for Production) ====================
# Protects Cron Job API endpoints
# Use a strong random string for production, e.g.: openssl rand -hex 32

CRON_SECRET=your_secure_random_string

# ==============================================================
# After configuration, start the development server:
# npm run dev
# ==============================================================

